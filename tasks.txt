replace char* with a struct alternative that supports alternate encodings
replace the keymaps with a union type im getting intersects with the shift combos like big H and big d
add ncurses rendering and split it off
add a config system
figure out and setup lsp system
finish vim bindings then add regular editor bindings
line numbers + control of them relative vs absolute
version control integration
firgure out and add an add-ons system
fix the exiting bug
create logging system and warning reporter
further split the project to allow for modularity for things such as ncurses
